# Buddhira Frontend — Environment (no hardcoded URLs; nothing sensitive in NEXT_PUBLIC_*)
# Copy to .env.local and set every value. Only publishable keys go in NEXT_PUBLIC_*.

# Required: backend API (e.g. Render). For local dev: http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=https://your-api.onrender.com

# Required for Supabase Auth (anon key is safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your_anon_key...

# Optional: password reset redirect (if different from app origin, e.g. custom domain)
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app

# Local dev example:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
