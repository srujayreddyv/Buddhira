# =============================================================================
# Buddhira Backend - Environment Variables
# =============================================================================
# Copy to .env and fill in your values.
# Get credentials from: https://supabase.com/dashboard/project/_/settings/api
#
# Cross-app: SUPABASE_URL must match frontend NEXT_PUBLIC_SUPABASE_URL (same project).
# CORS_ORIGINS must include your frontend origin (e.g. http://localhost:3000 in dev).
# =============================================================================

# Supabase (required). Same project URL as frontend — do not use service_role in frontend.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_KEY=eyJ...your_service_role_key...

# JWT verification uses JWKS from Supabase; no separate JWT secret needed.

# Allowed origins for CORS (must include frontend URL). Comma-separated.
# Dev: http://localhost:3000  — Production: add your frontend origin(s)
CORS_ORIGINS=http://localhost:3000

# Optional: version returned in /health (set on Render for deploy verification)
# APP_VERSION=1.0.0

# Optional: for backend/smoke_test.py (e.g. local E2E check)
# BACKEND_URL=http://localhost:8000
# SUPABASE_ANON_KEY=<same as frontend anon key>
# SMOKE_TEST_EMAIL=your-test-user@example.com
# SMOKE_TEST_PASSWORD=your-test-password
