# =============================================================================
# Buddhira Backend — Environment (no hardcoded URLs or secrets)
# =============================================================================
# Copy to .env and set every value. Get credentials from Supabase Dashboard → Settings → API.
# =============================================================================

# Required
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your_service_role_key...
# CORS: explicit origins only (no wildcard). Comma-separated: production + each preview URL you need.
CORS_ORIGINS=https://your-app.vercel.app

# Optional: override JWKS URL (default: SUPABASE_URL + /auth/v1/.well-known/jwks.json)
# SUPABASE_JWKS_URL=https://YOUR_PROJECT_REF.supabase.co/auth/v1/.well-known/jwks.json

# Optional: JWT claim validation (default audience is "authenticated")
# JWT_AUDIENCE=authenticated
# JWT_ISSUER=

# Optional: /health version
# APP_VERSION=1.0.0

# Production example (Vercel + preview): list each origin; no *.vercel.app pattern support
# CORS_ORIGINS=https://yourapp.vercel.app,https://yourapp-git-main-yourteam.vercel.app

# Local dev: use same project as frontend; CORS_ORIGINS must include frontend origin
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
# CORS_ORIGINS=http://localhost:3000

# Smoke test (optional)
# BACKEND_URL=http://localhost:8000
# SUPABASE_ANON_KEY=same as frontend anon key
# SMOKE_TEST_EMAIL=test@example.com
# SMOKE_TEST_PASSWORD=your-password
